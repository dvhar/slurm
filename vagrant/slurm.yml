
  - hosts: vcluster
    tasks: [ ]

  - hosts: first
    become: true
    roles:
    - { role: slurm, node_type: master }

  - hosts: second
    become: true
    roles:
    - { role: slurm, node_type: slave }
