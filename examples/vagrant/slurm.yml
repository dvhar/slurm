  - hosts: vcluster
    tasks: [ ]

  - hosts: controller
    become: true
    roles:
    - { role: RDI2.slurm, cluster: vcluster, node_type: master }

  - hosts: workers
    become: true
    roles:
    - { role: RDI2.slurm, cluster: vcluster, node_type: slave }
